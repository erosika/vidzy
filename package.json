{
  "name": "vidzy",
  "version": "0.1.0",
  "description": "Video production pipeline. Ingest, transcribe, analyze, plan, edit, soundtrack, reframe, filter, export.",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
